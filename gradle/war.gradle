apply plugin: "war"

bootWar {
    mainClassName = "com.gtx.internal.api.ApiApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
